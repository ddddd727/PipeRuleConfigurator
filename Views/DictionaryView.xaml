<UserControl x:Class="PipeRuleConfigurator.Views.DictionaryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:vm="clr-namespace:PipeRuleConfigurator.ViewModels"
             xmlns:local="clr-namespace:PipeRuleConfigurator.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1200">

    <UserControl.DataContext>
        <vm:DictionaryViewModel/>
    </UserControl.DataContext>

    <UserControl.Resources>
        <local:RowStateToBrushConverter x:Key="RowStateConverter"/>
    </UserControl.Resources>

    <Grid Background="{DynamicResource MaterialDesignPaper}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" BorderThickness="0,0,0,1" BorderBrush="{DynamicResource MaterialDesignDivider}">
            <StackPanel Orientation="Horizontal" Margin="5" HorizontalAlignment="Left">
                <Border BorderThickness="1" BorderBrush="{DynamicResource MaterialDesignDivider}" CornerRadius="4" Margin="2">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <ListBox Grid.Row="0" 
                                 ItemsSource="{Binding TreeNodes[0].Children}" 
                                 SelectedItem="{Binding SelectedTreeItem}"
                                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                 ScrollViewer.VerticalScrollBarVisibility="Disabled"
                                 BorderThickness="0"
                                 Background="Transparent"
                                 VerticalAlignment="Center">
                            <ListBox.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ListBox.ItemsPanel>
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem" BasedOn="{StaticResource MaterialDesignListBoxItem}">
                                    <Setter Property="Padding" Value="8,6"/>
                                    <Setter Property="Margin" Value="2"/>
                                    <Setter Property="Cursor" Value="Hand"/>
                                    <Setter Property="materialDesign:ListBoxItemAssist.CornerRadius" Value="4"/>
                                </Style>
                            </ListBox.ItemContainerStyle>
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <materialDesign:PackIcon Kind="{Binding IconKind}" Width="18" Height="18" VerticalAlignment="Center" Foreground="{DynamicResource PrimaryHueMidBrush}" Margin="0,0,6,0"/>
                                        <TextBlock Text="{Binding Title}" FontSize="13" VerticalAlignment="Center" Foreground="{DynamicResource MaterialDesignBody}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                        <Border Grid.Row="1" Background="{DynamicResource MaterialDesignDivider}" Padding="2">
                            <TextBlock Text="{Binding TreeNodes[0].Title}" FontSize="10" Foreground="{DynamicResource MaterialDesignBody}" Opacity="0.7" HorizontalAlignment="Center" FontWeight="Medium"/>
                        </Border>
                    </Grid>
                </Border>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="20,15,20,10" HorizontalAlignment="Left">
            <Button Command="{Binding AddRowCommand}" Content="增加行" Style="{StaticResource MaterialDesignOutlinedButton}" Height="30" Margin="0,0,10,0"/>
            <Button Command="{Binding AddColumnCommand}" Content="增加列" Style="{StaticResource MaterialDesignOutlinedButton}" Height="30" Margin="0,0,10,0"/>
            <Button Command="{Binding EditCommand}" Content="编辑" Style="{StaticResource MaterialDesignOutlinedButton}" Height="30" Margin="0,0,10,0"/>
            <Button Command="{Binding SaveCommand}" Content="保存" Style="{StaticResource MaterialDesignFlatMidBgButton}" Height="30"/>
        </StackPanel>

        <DataGrid Grid.Row="2" 
                  ItemsSource="{Binding TableData}" 
                  SelectedItem="{Binding SelectedRow, Mode=TwoWay}"
                  SelectionChanged="DataGrid_SelectionChanged"
                  LoadingRow="DataGrid_LoadingRow"
                  RowEditEnding="DataGrid_RowEditEnding"
                  AutoGenerateColumns="True" 
                  AutoGeneratingColumn="DataGrid_AutoGeneratingColumn"
                  CanUserAddRows="False"
                  HeadersVisibility="Column" 
                  SelectionUnit="FullRow"
                  IsReadOnly="False"
                  BorderThickness="1"
                  BorderBrush="{DynamicResource MaterialDesignDivider}"
                  Margin="20,0,20,20">

            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}">
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="Padding" Value="10,8"/>
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                </Style>
            </DataGrid.Resources>

            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow" BasedOn="{StaticResource MaterialDesignDataGridRow}">
                    <Setter Property="Background" Value="{Binding Converter={StaticResource RowStateConverter}}"/>
                </Style>
            </DataGrid.RowStyle>

            <DataGrid.Columns>
                <DataGridCheckBoxColumn Binding="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" 
                                        CanUserSort="False"
                                        ElementStyle="{StaticResource MaterialDesignDataGridCheckBoxColumnStyle}"
                                        EditingElementStyle="{StaticResource MaterialDesignDataGridCheckBoxColumnEditingStyle}">
                    <DataGridCheckBoxColumn.HeaderTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding DataContext.IsAllSelected, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"
                                      ToolTip="全选/取消全选"
                                      HorizontalAlignment="Center" 
                                      VerticalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridCheckBoxColumn.HeaderTemplate>
                </DataGridCheckBoxColumn>
            </DataGrid.Columns>

        </DataGrid>

    </Grid>
</UserControl>